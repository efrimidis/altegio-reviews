{
  "name": "altegio-reviews",
  "version": "1.0.0",
  "description": "Altegio-to-Telegram automation for reviews, free slots and daily payroll reports.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check altegio.js && node --check slots.js && node --check report.js && node --check telegram.js && node --check state.js && node --check security.js && node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "node-cron": "^4.2.1"
  }
}
